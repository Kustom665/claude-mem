{
  "name": "aios",
  "version": "0.1.0",
  "description": "AI OS - an operating system where AI agents are processes",
  "type": "module",
  "private": true,
  "bin": {
    "aios": "./src/main.ts"
  },
  "scripts": {
    "start": "bun run src/main.ts",
    "shell": "bun run src/main.ts shell",
    "web": "bun run src/main.ts web",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
